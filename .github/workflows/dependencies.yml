name: Check deps

on:
  pull_request:
    paths:
      - pyproject.toml
      - requirements.txt
      - requirements-dev.txt
      - docs/requirements.in
      - docs/requirements.txt
      - .github/workflows/dependencies.yml

env:
  DEBIAN_FRONTEND: noninteractive

jobs:
  quality:
    name: Checking dependency graph
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ['ubuntu-latest']
        python-version: ['3.10']

    permissions:
      contents: write
      actions: write

    steps:
      - uses: actions/checkout@v6
        if: github.event.pull_request.head.repo.full_name != github.repository
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/checkout@v6
        if: github.event.pull_request.head.repo.full_name == github.repository
        with:
          token: ${{ secrets.PAT }}
          ref: ${{ github.head_ref }}

      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          python-version: ${{ matrix.python-version }}

      - name: Check dependency graph
        run: |
          uv pip compile pyproject.toml -o requirements.txt
          uv pip compile pyproject.toml -c requirements.txt --extra dev -o requirements-dev.txt
          cd docs/ && uv pip compile -c ../requirements.txt -c ../requirements-dev.txt requirements.in -o requirements.txt

      - name: Verify dependency graph is ok
        uses: tj-actions/verify-changed-files@v20
        id: verify-changed-files
        with:
          files: |
            requirements.txt
            requirements-dev.txt
            docs/requirements.txt

      - name: Validating graph
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          echo "Dependency file(s) changed: ${{ steps.verify-changed-files.outputs.changed_files }}"
          git diff
          # core.setFailed('Please add your dependencies in pyproject.toml or docs/requirements.in, then regenerate the lockfiles. (see docs/contribute/development)')

      - name: Commit files changed
        uses: stefanzweifel/git-auto-commit-action@v7
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        with:
            # Optional. Commit message for the created commit.
            # Defaults to "Apply automatic changes"
            commit_message: "Fix deps"
