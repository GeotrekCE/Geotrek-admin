[buildout]
extends = 
    buildout.cfg
eggs +=
    mock

parts +=
    coverage

[coverage]
recipe = zc.recipe.egg
eggs = coverage

[django]
settings = settings.tests

[versions]
coverage = 4.5.3
mock = 3.0.5
