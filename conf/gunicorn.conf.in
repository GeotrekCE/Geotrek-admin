backlog = 2048
bind = "${options.bind}"
pidfile = "${options.pidfile}"
workers = ${options.workers}
logfile = "${options.errorlog}"
accesslog = "${options.errorlog}"
errorlog = "${options.errorlog}"
timeout = ${options.timeout}
debug = False
