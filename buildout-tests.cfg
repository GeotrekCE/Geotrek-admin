[buildout]
extends = 
    buildout.cfg

eggs +=
    django-jenkins
    pylint
    coverage


[django]
extra-settings +=
    INSTALLED_APPS += (
        'django_jenkins',
    )
    TEST_EXCLUDE = ('django',)
    PYLINT_RCFILE = os.path.join(PROJECT_ROOT_PATH, '..', '.pylint.rc')
