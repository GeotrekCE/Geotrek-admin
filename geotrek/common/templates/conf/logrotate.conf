{{ VAR_DIR }}/log/*.log {
    rotate 4
    su {{ user }}
    weekly
    create
    compress
    delaycompress
    sharedscripts
    postrotate
        for pidfile in {{ VAR_DIR }}/pid/*.pid; do kill -USR1 `cat $$pidfile`; done
    endscript
}
